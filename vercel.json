{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["fra1"],
  "installCommand": "npm install",
  "buildCommand": "NEXT_ADAPTER_PATH= npx prisma generate && NEXT_ADAPTER_PATH= npx next build --webpack && node scripts/ensure-nft.js"
}
